server:
  port: 0

spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      profile: docker
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: env, health, configprops